{% extends 'dashboard_base.html' %}
{% block title %}Candidatures reçues{% endblock %}
{% block content %}
<div class="container py-5">
    <h2 class="mb-4">Candidatures reçues</h2>
    {% if candidatures %}
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Offre</th>
                    <th>Candidat</th>
                    <th>Lettre</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for c in candidatures %}
                <tr>
                    <td>{{ c.offre.titre }}</td>
                    <td>{{ c.candidat.get_full_name }}</td>
                    <td>{{ c.lettre_motivation|linebreaksbr }}</td>
                    <td>{{ c.get_statut_display }}</td>
                    <td>
                        <form method="post" class="d-flex">
                            {% csrf_token %}
                            <input type="hidden" name="candidature_id" value="{{ c.id }}">
                            <select name="statut" class="form-select form-select-sm me-2">
                                {% for key, val in c.Statut.choices %}
                                    <option value="{{ key }}" {% if key == c.statut %}selected{% endif %}>{{ val }}</option>
                                {% endfor %}
                            </select>
                            <button type="submit" class="btn btn-sm btn-primary">Mettre à jour</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% else %}
    <p>Aucune candidature reçue.</p>
    {% endif %}
</div>
{% endblock %}
